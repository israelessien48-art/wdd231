from PIL import Image

# Open the source image (jpg or png)
img = Image.open("image.png").convert("RGBA")

# Save as favicon.ico with multiple sizes
img.save(
    "favicon.ico",
    format="ICO",
    sizes=[(16,16), (32,32), (48,48), (64,64), (128,128), (256,256)]
)

